import styles from './Hero.module.css'

function Hero() {
    return (
        <section className={styles.heroContainer}>
           <h1>Hero Section</h1>
        </section>
    )
}

export default Hero
